[phases.setup]
nixPkgs = [
  "nodejs_20",
  "python311",
  "python311Packages.pip"
]

aptPkgs = [
  "poppler-utils",
  "tesseract-ocr"
]

[phases.install]
cmds = [
  "npm ci || npm install",
  "pip install -r requirements.txt"
]

[start]
cmd = "node server.js"